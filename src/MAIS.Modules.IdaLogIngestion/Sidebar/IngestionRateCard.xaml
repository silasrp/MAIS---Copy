<UserControl x:Class="MAIS.Modules.IdaLogIngestion.Sidebar.IngestionRateCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MAIS.Modules.IdaLogIngestion.Sidebar">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Border Margin="8,4"
            Background="#16213E"
            BorderBrush="#2A3A5C"
            BorderThickness="1"
            CornerRadius="8"
            ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ── Title bar ── -->
            <Border Grid.Row="0" Background="#0F172A" Padding="10,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Connected / disconnected indicator -->
                    <Ellipse Grid.Column="0" Width="7" Height="7"
                             VerticalAlignment="Center" Margin="0,0,7,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#4A5568"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Fill" Value="#34D399"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>

                    <TextBlock Grid.Column="1"
                               Text="{Binding DisplayName}"
                               Foreground="#8B95B0" FontSize="10" FontWeight="SemiBold"
                               FontFamily="Segoe UI" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>

                    <!-- Pending review badge -->
                    <Border Grid.Column="2" CornerRadius="8" Padding="5,1"
                            Background="#6C8EFF26" VerticalAlignment="Center"
                            Visibility="{Binding HasPendingReviews, Converter={StaticResource BoolToVis}}">
                        <TextBlock Foreground="#6C8EFF" FontSize="9" FontWeight="SemiBold">
                            <Run Text="{Binding PendingReviewCount}"/>
                            <Run Text=" to review"/>
                        </TextBlock>
                    </Border>
                </Grid>
            </Border>

            <!-- ── Rate summary ── -->
            <Border Grid.Row="1" Padding="10,8">
                <TextBlock Text="{Binding RateSummary}"
                           FontSize="10" Foreground="#8B95B0"
                           FontFamily="Segoe UI" TextWrapping="Wrap"/>
            </Border>

            <!-- ── Status strip ── -->
            <Border Grid.Row="2" Background="#0A1020" Padding="10,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Live — log ingestion rate"
                               Foreground="#4A5568" FontSize="9" FontFamily="Segoe UI"
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                    <Button Grid.Column="1"
                            Content="Review →"
                            Command="{Binding OpenReviewPanelCommand}"
                            Height="20" FontSize="9" Padding="6,2" Cursor="Hand"
                            Background="Transparent" Foreground="#8B95B0"
                            BorderBrush="#2A3A5C" BorderThickness="1">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#1E2D4A"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
